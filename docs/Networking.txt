VPC (10.0.0.0/16)
├── Public Subnets (AZ-a, AZ-b)
│   ├── Internet Gateway
│   ├── Application Load Balancer (CloudFront Origin)
│   └── Regional NAT Gateway (Egress for Private Subnets)
│
└── Private Subnets (AZ-a, AZ-b)
    ├── ECS EC2 instances (Odoo nodes & PgBouncer nodes)
    ├── ECS Service Connect (Discovery: pgbouncer.odoo.local)
    ├── ECS Task ENIs (Actual Odoo/PgBouncer instances)
    ├── RDS PostgreSQL (Database)
    └── EFS (Shared Storage)
